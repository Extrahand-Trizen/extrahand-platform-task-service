/** Minimum service subtotal (pre-GST, pre-coupon) for fixed-price Book Now checkout. */
export const BOOK_NOW_FIXED_PRICE_MIN_CHECKOUT_RUPEES = 199;

export const BOOK_NOW_MIN_CHECKOUT_NOT_MET_CODE = 'BOOK_NOW_MIN_CHECKOUT_NOT_MET' as const;
